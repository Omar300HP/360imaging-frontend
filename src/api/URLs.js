// api and socket urls

const api_base_url = process.env.REACT_APP_HOST_IP_ADDRESS;

export const api_url = api_base_url + "/api/v1";

export const download_url = api_base_url;

export const socket_url = api_base_url;
